{
  "name": "wire-mcp",
  "type": "module",
  "private": true,
  "description": "Stateless MCP server (Cloudflare Worker) over wire.phall.io's live JSON.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^6.0.3",
    "wrangler": "^4.0.0"
  }
}
